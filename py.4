The implementation involves reading the input values 
𝑋
X and 
𝑌
Y, calculating the total number of packets, and then printing the result.

X, Y = map(int, input().strip().split())
max_customers = X * Y
print(max_customers)
